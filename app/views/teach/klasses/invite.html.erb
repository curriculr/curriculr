

<%= form_for @invitation, :url => invite_teach_course_klass_path(@course, @klass), :method => :post do |f| %>
  <%= f.static :course, value: @course.name %>
  <%= f.static :klass, value: @klass.name %>
  <%= f.email_field :invitee %>

  <%= f.submit %>
  <%= f.cancel home_path %>
<% end %>

