<%= ui_header(t('helpers.submit.site_settings'), action: capture do %>
  <div class="ui mini menu">
    <%= link(:settings, :close, home_path, class: "item") %>
  </div>
<% end ) %>

<%=
  html = ''
  json_settings_tree(configure_admin_account_path(@account, setting: '_key_'), nil, @account.config, html, [], 1, [])
  html.html_safe 
%>
